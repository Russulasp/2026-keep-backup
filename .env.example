# Path to an existing logged-in Chromium profile directory used by Playwright.
# This directory is an external asset and must stay outside this repository.
# Local run: this absolute path is used directly.
# Docker run: this path is bind-mounted to /keep-profile and used from there.
# Leave empty to run no-profile smoke mode (CI default).
KEEP_BROWSER_PROFILE_DIR=
